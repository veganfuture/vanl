# No [server] section: the listen address is controlled by the standard
# PORT/HOST environment variables (nitro's convention, set in the systemd
# unit — see flake.nix), not this file.

[site]
base_url = "https://veganactivists.nl"

[logging]
level = "info"

[database]
host = "127.0.0.1"
port = 5432
database = "vanl"
user = "vanl"

[auth]
bot_api_base_url = "http://127.0.0.1:8787"
# Public half of the production signup keypair, generated with
# `nix run ./bot#generate-signup-key` on 2026-08-30. The matching private half
# is set as VANL_SIGNUP_PRIVATE_KEY in /etc/vanl/bot.env on the VPS (never
# committed).
signup_public_key = "U0xyzhWb_DYvbg_0IEFlGkIm2E4EhkN-j9GoQ7HJtmo"
